node_modules
.next
out
build
.env
.env.local
.env.*.local
.vercel
.DS_Store
*.log
.turbo
coverage
.pnpm-store
# India basemap is committed (1MB public-domain Datameet GeoJSON) so
# Vercel never depends on a network fetch at build time. The /map page
# hard-depends on it — sourcing new atlas records must not be coupled
# to upstream maintainers staying online.
# public/geo/india-states.json
# public/geo/india-states-topo.json
tsconfig.tsbuildinfo
.data/

# Bulky source illustration set (only the optimised copies in public/illustrations are tracked)
public/cat. hub illustration/
